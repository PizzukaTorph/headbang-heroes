namespace HeadbangHeroes.Charts.Runtime
{
    /// <summary>Canonical neck technique vocabulary (FOUNDATION.md). Extension must not change existing meaning.</summary>
    public enum NeckTechnique
    {
        Classic,
        Half,
        Deep,
        Whiplash,
        Windmill
    }

    /// <summary>Canonical trajectory vocabulary.</summary>
    public enum NeckTrajectory
    {
        Horizontal,
        Vertical,
        Circular,
        CenterEdge
    }

    /// <summary>Canonical modifier vocabulary.</summary>
    public enum NeckModifier
    {
        None,
        Double,
        Hold,
        Accent,
        Burst
    }
}
